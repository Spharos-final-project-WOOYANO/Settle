server:
  port: 0

spring:
  application:
    name: settle-service
  profiles:
    default: secret

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/settle?serverTimezone=Asia/Seoul
    username: pjm
    password: jmjm1102

    jpa:
      hibernate:
        ddl-auto: update
      properties:
        hibernate:
          # show_sql: true
          format_sql: true
  logging.level:
    org.hibernate.SQL: debug
